#include<stdio.h>
int main()
{
    int p,j,n,s=0;
    printf(" enter the no ");
    scanf("%d",&n);
    for(p=1;p<=n;p++)
      {
        printf("Enter the no=%d: ",i);
        scanf("%d",&j);
        s=s+j;
      }
  printf("sum of number= %d",s);
  return 0;
}